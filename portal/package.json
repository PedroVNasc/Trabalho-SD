{
    "name": "portal",
    "version": "1.0.0",
    "main": "dist/portal.js",
    "scripts": {
        "build": "tsc",
        "start": "node dist/portal.js",
        "dev": "ts-node-dev --respawn --transpile-only src/portal.ts",
        "release": "pnpm run build && pnpm run start"
    },
    "devDependencies": {
        "@types/express": "^4.17.21",
        "@types/node": "^20.14.9",
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.5.2"
    },
    "dependencies": {
        "express": "^4.19.2"
    }
}
